# Em dev o Vite faz proxy de /api para o Fastify local (ver vite.config.ts).
# Em producao o rewrite do vercel.json aponta para o projeto da API,
# o que mantem /api same-origin e o cookie de refresh sem SameSite=None.
VITE_API_URL=/api
